// num_comm_params=0
// num_mand_comm_params=0
// num_opt_comm_params=0

// git_link=https://github.com/jaegertracing/jaeger/blob/12bba8c9b91cf4a29d314934bc08f4a80e43c042/plugin/sampling/strategystore/static/strategy_store_test.go#L415
typedef Wgdef {
	chan update = [0] of {int};
	chan wait = [0] of {int};
	int Counter = 0;}
typedef Mutexdef {
	chan Lock = [0] of {bool};
	chan Unlock = [0] of {bool};
	chan RLock = [0] of {bool};
	chan RUnlock = [0] of {bool};
	int Counter = 0;}



init { 
	chan child_TestAutoUpdateStrategyErrors4150 = [1] of {int};
	run TestAutoUpdateStrategyErrors415(child_TestAutoUpdateStrategyErrors4150);
	run receiver(child_TestAutoUpdateStrategyErrors4150)
stop_process:skip
}

proctype TestAutoUpdateStrategyErrors415(chan child) {
	bool closed; 
	int i;
	bool state;
	int num_msgs;
	Mutexdef mockServer_mu;
	Wgdef mockServer_wg;
	Mutexdef mockServer_Config_mu;
	Mutexdef mockServer_TLS_mutex;
	Mutexdef logs_mu;
	run mutexMonitor(logs_mu);
	run mutexMonitor(mockServer_TLS_mutex);
	run mutexMonitor(mockServer_Config_mu);
	run wgMonitor(mockServer_wg);
	run mutexMonitor(mockServer_mu);
		stop_process: skip;
		stop_process: skip;
	stop_process: skip;
	child!0
}

 /* ================================================================================== */
 /* ================================================================================== */
 /* ================================================================================== */ 
proctype wgMonitor(Wgdef wg) {
int i;
do
	:: wg.update?i ->
		wg.Counter = wg.Counter + i;
		assert(wg.Counter >= 0)
	:: wg.Counter == 0 ->
end: if
		:: wg.update?i ->
			wg.Counter = wg.Counter + i;
			assert(wg.Counter >= 0)
		:: wg.wait!0;
	fi
od
}

proctype mutexMonitor(Mutexdef m) {
bool locked = false;
do
:: true ->
	if
	:: m.Counter > 0 ->
		if 
		:: m.RUnlock?false -> 
			m.Counter = m.Counter - 1;
		:: m.RLock?false -> 
			m.Counter = m.Counter + 1;
		fi;
	:: locked ->
		m.Unlock?false;
		locked = false;
	:: else ->	 end:	if
		:: m.Unlock?false ->
			assert(0==32);		:: m.Lock?false ->
			locked =true;
		:: m.RUnlock?false ->
			assert(0==32);		:: m.RLock?false ->
			m.Counter = m.Counter + 1;
		fi;
	fi;
od
}

proctype receiver(chan c) {
c?0
}

